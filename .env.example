# ============================================
# Leaf Disease AI - 環境變數配置範例
# ============================================
# 複製此檔案為 .env 並填入實際值
# cp .env.example .env

# ============================================
# 應用程式設定（必填）
# ============================================
# Flask 應用程式密鑰（用於 session 加密）
# 請使用強隨機字串，例如：openssl rand -hex 32
SECRET_KEY=your-secret-key-here

# 應用程式環境（development, production, testing）
FLASK_ENV=development
ENV=development

# 是否啟用除錯模式（開發環境建議 true，生產環境必須 false）
DEBUG=true

# session 過期時間 單位：小時 (自訂版)
# 預設會從 資料庫 session 抓（ 24小時 ）
# PERMANENT_SESSION_LIFETIME_HOURS =

# ============================================
# PostgreSQL 資料庫設定（必填）
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_NAME=leaf_disease_ai
DB_USER=postgres
DB_PASSWORD=your_database_password_here

# ============================================
# Redis 快取設定（可選，但強烈建議）
# ============================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ============================================
# Cloudinary 圖片儲存設定（強烈建議啟用）
# ============================================
# 是否啟用 Cloudinary（true/false）
# 啟用後，所有圖片將儲存在 Cloudinary，不再儲存在資料庫
USE_CLOUDINARY=true

# Cloudinary 帳號資訊（從 Cloudinary Dashboard 獲取）
# https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=your_cloud_name_here
CLOUDINARY_API_KEY=your_api_key_here
CLOUDINARY_API_SECRET=your_api_secret_here

# 是否使用 HTTPS（建議 true）
CLOUDINARY_SECURE=true

# Cloudinary 儲存資料夾路徑
CLOUDINARY_FOLDER=leaf_disease_ai

# ============================================
# AI 模型路徑設定（可選，有預設值）
# ============================================
# CNN 模型路徑（model/CNN/...）
CNN_MODEL_PATH_RELATIVE=model/CNN/CNN_v1.0_20251204/best_mobilenetv3_large.pth

# YOLO 模型路徑（model/yolov11/...）
YOLO_MODEL_PATH_RELATIVE=model/yolov11/best_v1_50.pt

# ============================================
# Swagger API 文檔設定（可選）
# ============================================
SWAGGER_HOST=localhost:5000
SWAGGER_TITLE=Leaf Disease AI API
SWAGGER_DESCRIPTION=葉片病害檢測 AI 系統 API 文檔
SWAGGER_VERSION=2.0.0
SWAGGER_BASE_PATH=/
SWAGGER_SCHEMES=http

# ============================================
# 上傳設定（可選，有預設值）
# ============================================
# 上傳資料夾路徑（相對於專案根目錄）
UPLOAD_FOLDER_RELATIVE=uploads

# 最大上傳檔案大小（位元組，預設 5MB）
MAX_CONTENT_LENGTH=5242880

# ============================================
# 會話設定（可選）
# ============================================
# 會話過期時間（小時，預設 1 小時）
PERMANENT_SESSION_LIFETIME_HOURS=1

# 會話 Cookie 安全設定（生產環境建議 true）
SESSION_COOKIE_SECURE=false
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# ============================================
# 日誌設定（可選）
# ============================================
# 日誌檔案路徑（相對於專案根目錄）
LOG_FILE=data/logs/app.log

# 日誌檔案最大大小（位元組，預設 10MB）
LOG_MAX_SIZE=10485760

# 日誌備份數量（預設 10）
LOG_BACKUP_COUNT=10

# ============================================
# 快取設定（可選）
# ============================================
# 預設快取過期時間（秒，預設 3600 秒 = 1 小時）
CACHE_DEFAULT_TIMEOUT=3600
