# Python 後端依賴
# 注意：前端依賴（React、Tailwind CSS 等）在 frontend/package.json 中管理
# 安裝前端依賴：cd frontend && npm install

# 基礎依賴
Flask
psycopg2-binary
python-dotenv
werkzeug
Pillow
ultralytics
numpy
opencv-python
cloudinary
requests

# Redis 和快取
redis
flask-caching

# API 文檔
flask-swagger-ui
flasgger

# 開發依賴
pytest
pytest-cov
black
flake8
